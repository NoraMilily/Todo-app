# Технические детали реализации Frontend и Backend

## 📋 Содержание
1. [Архитектура Next.js App Router](#архитектура-nextjs-app-router)
2. [Server Components vs Client Components](#server-components-vs-client-components)
3. [Server Actions - механизм работы](#server-actions---механизм-работы)
4. [Работа с базой данных через Prisma](#работа-с-базой-данных-через-prisma)
5. [Система аутентификации NextAuth](#система-аутентификации-nextauth)
6. [Поток данных в приложении](#поток-данных-в-приложении)
7. [Безопасность и валидация](#безопасность-и-валидация)

---

## 🏗️ Архитектура Next.js App Router

### Что такое App Router?

App Router — это новая система маршрутизации в Next.js 13+, которая использует структуру папок для определения маршрутов.

### Структура маршрутов в нашем проекте:

```
src/app/
├── [locale]/              # Динамический сегмент для языка (ru, en)
│   ├── page.tsx          # Маршрут: /ru или /en
│   ├── profile/
│   │   ├── page.tsx      # Маршрут: /ru/profile
│   │   └── edit/
│   │       └── page.tsx  # Маршрут: /ru/profile/edit
│   └── auth/
│       ├── login/
│       │   └── page.tsx  # Маршрут: /ru/auth/login
│       └── register/
│           └── page.tsx  # Маршрут: /ru/auth/register
└── actions/              # Server Actions (не маршруты!)
    ├── todos.ts
    ├── auth.ts
    └── profile.ts
```

### Как работает маршрутизация:

1. **Файл `page.tsx`** = страница на этом маршруте
2. **Папка `[locale]`** = динамический сегмент (может быть `ru`, `en`, и т.д.)
3. **Вложенные папки** = вложенные маршруты

### Пример: Запрос `/ru/profile`

```
1. Next.js ищет файл: app/[locale]/profile/page.tsx
2. Извлекает параметр: locale = "ru"
3. Выполняет Server Component (page.tsx)
4. Возвращает HTML с данными
```

---

## 🖥️ Server Components vs Client Components

### Server Components (Серверные компоненты)

**Характеристики:**
- Выполняются **только на сервере**
- Имеют прямой доступ к базе данных
- Не могут использовать браузерные API (`localStorage`, `window`, и т.д.)
- Не могут использовать React хуки (`useState`, `useEffect`)
- Меньше JavaScript отправляется в браузер

**Пример из проекта:**

```typescript
// src/app/[locale]/page.tsx
// Это Server Component (нет директивы "use client")

import { auth } from "@/lib/auth";
import { prisma } from "@/lib/prisma";
import { TodoApp } from "@/components/TodoApp";

export default async function HomePage({ params }) {
  // ✅ Прямой доступ к базе данных на сервере
  const session = await auth();
  const todos = await prisma.todo.findMany({
    where: { userId: session.user.id }
  });

  // ✅ Данные уже загружены, когда страница отправляется в браузер
  return <TodoApp todos={todos} />;
}
```

**Преимущества:**
- Быстрая загрузка (данные уже в HTML)
- Безопасность (код не попадает в браузер)
- Меньше JavaScript = быстрее работает

### Client Components (Клиентские компоненты)

**Характеристики:**
- Выполняются **в браузере пользователя**
- Могут использовать React хуки
- Могут использовать браузерные API
- Отвечают за интерактивность

**Пример из проекта:**

```typescript
// src/components/TodoApp.tsx
"use client"; // ✅ Директива указывает, что это Client Component

import { useState, useTransition } from "react";

export function TodoApp({ todos }) {
  // ✅ Можно использовать хуки (только в Client Components)
  const [filter, setFilter] = useState("all");
  const [isPending, startTransition] = useTransition();

  // ✅ Обработчик событий (работает в браузере)
  const handleClick = () => {
    setFilter("active");
  };

  return (
    <div>
      <button onClick={handleClick}>Фильтр</button>
      {/* ... */}
    </div>
  );
}
```

**Когда использовать:**
- Нужна интерактивность (клики, ввод текста)
- Нужны анимации
- Нужен доступ к браузерным API
- Нужно управление состоянием

### Правило композиции:

```
Server Component
  └──> Может импортировать Client Component
       └──> Client Component НЕ может импортировать Server Component
            └──> Но может получать Server Component как children
```

**Пример:**

```typescript
// ✅ Правильно: Server Component → Client Component
// page.tsx (Server)
export default function Page() {
  return <TodoApp todos={todos} />; // TodoApp - Client Component
}

// ❌ Неправильно: Client Component → Server Component
"use client";
import ServerComponent from "./ServerComponent"; // Ошибка!
```

---

## ⚡ Server Actions - механизм работы

### Что такое Server Actions?

Server Actions — это функции, которые выполняются **только на сервере** и могут быть вызваны напрямую из Client Components.

### Синтаксис:

```typescript
// src/app/actions/todos.ts
"use server"; // ✅ Директива указывает, что это Server Action

export async function addTodoAction(formData: FormData) {
  // Этот код выполняется ТОЛЬКО на сервере
  const userId = await requireUserId();
  const text = formData.get("text");

  await prisma.todo.create({
    data: { text, userId }
  });

  revalidatePath("/");
}
```

### Как вызывается из Client Component:

```typescript
// src/components/TodoApp.tsx
"use client";

import { addTodoAction } from "@/app/actions/todos";

export function TodoApp() {
  return (
    <form action={addTodoAction}>
      {/* Next.js автоматически создает HTTP запрос */}
      <input name="text" />
      <button type="submit">Добавить</button>
    </form>
  );
}
```

### Что происходит под капотом:

```
1. Пользователь отправляет форму
   ↓
2. Next.js сериализует FormData
   ↓
3. Создается HTTP POST запрос к /_next/action/[id]
   ↓
4. Next.js выполняет Server Action на сервере
   ↓
5. Server Action обращается к базе данных
   ↓
6. revalidatePath() обновляет кэш
   ↓
7. Next.js автоматически обновляет страницу
```

### Преимущества Server Actions:

1. **Типобезопасность**
   ```typescript
   // TypeScript знает типы параметров и возвращаемого значения
   const result = await addTodoAction(formData);
   ```

2. **Автоматическая сериализация**
   - Next.js автоматически преобразует данные
   - Не нужно вручную создавать API endpoints

3. **Прогрессивное улучшение**
   - Работает даже без JavaScript (fallback на обычную форму)

4. **Безопасность**
   - Код выполняется только на сервере
   - Автоматическая защита от CSRF

---

## 🗄️ Работа с базой данных через Prisma

### Что такое Prisma?

Prisma — это **ORM (Object-Relational Mapping)** — инструмент, который позволяет работать с базой данных, используя код на TypeScript вместо SQL.

### Схема базы данных (Prisma Schema):

```prisma
// prisma/schema.prisma

model User {
  id          String   @id @default(cuid())
  email       String?  @unique
  username    String   @unique
  passwordHash String
  displayName String?
  avatarUrl   String?
  todos       Todo[]   // Связь один-ко-многим
}

model Todo {
  id        String   @id @default(cuid())
  text      String
  completed Boolean  @default(false)
  userId    String
  user      User     @relation(fields: [userId], references: [id])
  createdAt DateTime @default(now())
}
```

### Генерация Prisma Client:

```bash
npx prisma generate
```

Это создает TypeScript типы на основе схемы:

```typescript
// Автоматически сгенерированные типы
type User = {
  id: string;
  email: string | null;
  username: string;
  // ...
  todos: Todo[];
}

type Todo = {
  id: string;
  text: string;
  completed: boolean;
  // ...
}
```

### Использование Prisma в коде:

```typescript
// src/lib/prisma.ts
import { PrismaClient } from "@prisma/client";

export const prisma = new PrismaClient();
```

```typescript
// Примеры запросов

// 1. Создание записи
await prisma.todo.create({
  data: {
    text: "Новая задача",
    userId: "user123"
  }
});

// 2. Чтение записей
const todos = await prisma.todo.findMany({
  where: { userId: "user123" },
  orderBy: { createdAt: "desc" }
});

// 3. Обновление записи
await prisma.todo.update({
  where: { id: "todo123" },
  data: { completed: true }
});

// 4. Удаление записи
await prisma.todo.delete({
  where: { id: "todo123" }
});
```

### Миграции базы данных:

```bash
# Создать миграцию
npx prisma migrate dev --name add_username_displayname

# Применить миграции
npx prisma migrate deploy
```

Prisma автоматически создает SQL файлы:

```sql
-- prisma/migrations/xxx_add_username_displayname/migration.sql
ALTER TABLE "User" ADD COLUMN "username" TEXT;
ALTER TABLE "User" ADD COLUMN "displayName" TEXT;
```

### Преимущества Prisma:

1. **Типобезопасность** — ошибки видны на этапе компиляции
2. **Автоматическая генерация типов** — не нужно писать типы вручную
3. **Защита от SQL-инъекций** — автоматическая экранизация
4. **Миграции** — версионирование схемы БД
5. **Интуитивный API** — легко читать и писать

---

## 🔐 Система аутентификации NextAuth

### Что такое NextAuth?

NextAuth — это библиотека для аутентификации в Next.js приложениях. Она управляет сессиями, токенами и защитой маршрутов.

### Конфигурация NextAuth:

```typescript
// src/lib/auth.ts
import NextAuth from "next-auth";
import Credentials from "next-auth/providers/credentials";

export const { handlers, auth, signIn, signOut } = NextAuth({
  session: { strategy: "jwt" }, // Используем JWT токены
  providers: [
    Credentials({
      async authorize(credentials) {
        // Проверка логина и пароля
        const user = await prisma.user.findFirst({
          where: { email: credentials.email }
        });

        if (user && verifyPassword(credentials.password, user.passwordHash)) {
          return {
            id: user.id,
            email: user.email,
            // ...
          };
        }
        return null;
      }
    })
  ],
  callbacks: {
    async jwt({ token, user }) {
      // Обновление токена
      if (user) {
        token.id = user.id;
      }
      return token;
    },
    async session({ session, token }) {
      // Обновление сессии
      session.user.id = token.id;
      return session;
    }
  }
});
```

### Как работает аутентификация:

```
1. Пользователь вводит логин/пароль
   ↓
2. NextAuth вызывает authorize()
   ↓
3. Проверка пароля через bcrypt
   ↓
4. Если верно → создается JWT токен
   ↓
5. Токен сохраняется в cookie
   ↓
6. При каждом запросе NextAuth проверяет токен
   ↓
7. Если токен валиден → пользователь авторизован
```

### Использование в Server Components:

```typescript
// src/app/[locale]/page.tsx
import { auth } from "@/lib/auth";

export default async function Page() {
  const session = await auth();

  if (!session?.user) {
    redirect("/login");
  }

  // Пользователь авторизован
  return <div>Привет, {session.user.email}!</div>;
}
```

### Использование в Client Components:

```typescript
"use client";
import { useSession } from "next-auth/react";

export function Component() {
  const { data: session } = useSession();

  if (session) {
    return <div>Вы вошли как {session.user.email}</div>;
  }

  return <div>Вы не авторизованы</div>;
}
```

### JWT токены:

JWT (JSON Web Token) — это способ хранения информации о пользователе в зашифрованном виде.

**Структура JWT:**
```
header.payload.signature

header: { alg: "HS256", typ: "JWT" }
payload: { id: "user123", email: "user@example.com", exp: 1234567890 }
signature: HMACSHA256(base64UrlEncode(header) + "." + base64UrlEncode(payload), secret)
```

**Преимущества:**
- Не нужно хранить сессии в базе данных
- Быстрая проверка (не нужен запрос к БД)
- Масштабируемость

---

## 🔄 Поток данных в приложении

### Пример 1: Загрузка страницы со списком задач

```
┌─────────────┐
│   Браузер   │
│  (Клиент)   │
└──────┬──────┘
       │ 1. Запрос GET /ru
       ↓
┌──────────────────┐
│  Next.js Server  │
└──────┬───────────┘
       │ 2. Выполнение Server Component
       ↓
┌──────────────────┐
│  page.tsx        │
│  (Server)        │
│  - auth()         │
│  - prisma.todo... │
└──────┬───────────┘
       │ 3. Запрос к БД
       ↓
┌──────────────────┐
│  PostgreSQL      │
│  (База данных)   │
└──────┬───────────┘
       │ 4. Возврат данных
       ↓
┌──────────────────┐
│  page.tsx         │
│  - Генерация HTML │
└──────┬───────────┘
       │ 5. HTML + данные
       ↓
┌──────────────────┐
│  Браузер         │
│  - Рендер HTML   │
│  - Загрузка JS   │
│  - TodoApp.tsx   │
│    (Client)       │
└──────────────────┘
```

### Пример 2: Добавление новой задачи

```
┌─────────────┐
│   Браузер   │
│  (Клиент)   │
│             │
│  TodoApp.tsx│
│  (Client)   │
│  <form>     │
└──────┬──────┘
       │ 1. Submit формы
       ↓
┌──────────────────┐
│  Next.js Server  │
│  - HTTP POST      │
│  - /_next/action/ │
└──────┬───────────┘
       │ 2. Выполнение Server Action
       ↓
┌──────────────────┐
│  addTodoAction    │
│  (Server Action) │
│  - requireUserId()│
│  - Валидация      │
│  - prisma.create()│
└──────┬───────────┘
       │ 3. Запрос к БД
       ↓
┌──────────────────┐
│  PostgreSQL      │
│  - INSERT INTO   │
└──────┬───────────┘
       │ 4. Успешное сохранение
       ↓
┌──────────────────┐
│  addTodoAction   │
│  - revalidatePath│
└──────┬───────────┘
       │ 5. Обновление кэша
       ↓
┌──────────────────┐
│  Next.js Server  │
│  - Обновление    │
│    страницы      │
└──────┬───────────┘
       │ 6. Новые данные
       ↓
┌──────────────────┐
│  Браузер         │
│  - Обновление UI │
│  - Новая задача  │
│    видна         │
└──────────────────┘
```

---

## 🛡️ Безопасность и валидация

### 1. Валидация данных (Zod)

```typescript
import { z } from "zod";

// Схема валидации
const todoSchema = z.object({
  text: z.string()
    .min(1, "Текст обязателен")
    .max(200, "Максимум 200 символов")
});

// Использование
export async function addTodoAction(formData: FormData) {
  const text = formData.get("text");

  // Валидация с обработкой ошибок
  const result = todoSchema.safeParse({ text });

  if (!result.success) {
    return { ok: false, errors: result.error.flatten() };
  }

  // result.data.text - гарантированно валидный текст
  await prisma.todo.create({
    data: { text: result.data.text }
  });
}
```

### 2. Защита от SQL-инъекций

Prisma автоматически экранирует все запросы:

```typescript
// ✅ Безопасно - Prisma экранирует автоматически
await prisma.todo.findMany({
  where: { text: userInput } // userInput может быть любым
});

// ❌ Опасно - если бы использовали нативный SQL
// await db.query(`SELECT * FROM todos WHERE text = '${userInput}'`);
// Если userInput = "'; DROP TABLE todos; --"
// Это привело бы к удалению таблицы!
```

### 3. Хеширование паролей (bcrypt)

```typescript
// src/lib/password.ts
import bcrypt from "bcryptjs";

// При регистрации
export async function hashPassword(password: string): Promise<string> {
  return bcrypt.hash(password, 10); // 10 - количество раундов
}

// При входе
export async function verifyPassword(
  password: string,
  hash: string
): Promise<boolean> {
  return bcrypt.compare(password, hash);
}
```

**Почему bcrypt:**
- Одностороннее хеширование (нельзя восстановить пароль)
- Соль (salt) добавляется автоматически
- Медленное хеширование защищает от brute-force

### 4. Защита маршрутов

```typescript
// Middleware проверяет авторизацию
// src/middleware.ts
export function middleware(request: NextRequest) {
  const session = await auth();

  if (!session && request.nextUrl.pathname.startsWith("/profile")) {
    return NextResponse.redirect(new URL("/login", request.url));
  }
}

// Server Actions проверяют авторизацию
export async function addTodoAction(formData: FormData) {
  const userId = await requireUserId(); // Выбросит ошибку, если не авторизован

  // Только авторизованные пользователи могут добавлять задачи
  await prisma.todo.create({
    data: { userId, text: "..." }
  });
}
```

### 5. CORS и CSRF защита

NextAuth автоматически защищает от:
- **CSRF (Cross-Site Request Forgery)** — через токены
- **XSS (Cross-Site Scripting)** — через Content Security Policy
- **CORS** — настройки в Next.js

---

## 📊 Сравнение с традиционным подходом

### Традиционный подход (REST API):

```
Frontend (React)
  ↓ HTTP запрос
Backend API (Express.js)
  ↓ SQL запрос
Database (PostgreSQL)
  ↓ JSON ответ
Backend API
  ↓ JSON ответ
Frontend
  ↓ Обновление состояния
UI обновлен
```

**Проблемы:**
- Нужно создавать API endpoints вручную
- Дублирование типов (TypeScript типы в frontend и backend)
- Больше кода для поддержки
- Нет типобезопасности между frontend и backend

### Наш подход (Next.js Server Actions):

```
Frontend (React)
  ↓ Прямой вызов функции
Server Action (Next.js)
  ↓ Prisma запрос
Database (PostgreSQL)
  ↓ Автоматическое обновление
Frontend
  ↓ UI обновлен автоматически
```

**Преимущества:**
- Меньше кода
- Типобезопасность между frontend и backend
- Автоматическое обновление UI
- Встроенная защита

---

## 🎯 Заключение

Наш проект использует современный стек технологий:

1. **Next.js 16** — объединяет frontend и backend
2. **React 19** — современный UI фреймворк
3. **Prisma** — типобезопасная работа с БД
4. **PostgreSQL** — надежная реляционная БД
5. **NextAuth** — безопасная аутентификация
6. **TypeScript** — типобезопасность на всех уровнях

Это позволяет создавать быстрые, безопасные и масштабируемые веб-приложения с минимальным количеством кода.
