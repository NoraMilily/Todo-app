# Объяснение Frontend и Backend для курсовой работы

## 📚 Содержание
1. [Простое объяснение (для гуманитариев)](#простое-объяснение)
2. [Техническое объяснение](#техническое-объяснение)
3. [Архитектура проекта](#архитектура-проекта)
4. [Поток данных](#поток-данных)

---

## 🎯 Простое объяснение

### Что такое Frontend и Backend?

Представьте ресторан:
- **Frontend (Фронтенд)** — это зал ресторана, где сидят гости. Это то, что видит пользователь: кнопки, формы, красивые страницы. Это "лицо" приложения.
- **Backend (Бэкенд)** — это кухня ресторана, куда гости не заходят. Здесь происходит вся "магия": сохранение данных, проверка паролей, работа с базой данных.

### Как они работают вместе?

1. **Пользователь видит интерфейс (Frontend)** — страницу со списком задач
2. **Пользователь нажимает кнопку** — например, "Добавить задачу"
3. **Frontend отправляет запрос в Backend** — "Пожалуйста, сохрани эту задачу"
4. **Backend обрабатывает запрос** — проверяет, что пользователь авторизован, сохраняет задачу в базу данных
5. **Backend отправляет ответ обратно** — "Готово, задача сохранена!"
6. **Frontend обновляет интерфейс** — показывает новую задачу в списке

### В нашем проекте:

**Frontend делает:**
- Показывает красивый интерфейс с задачами
- Реагирует на действия пользователя (клики, ввод текста)
- Анимирует переходы между страницами
- Показывает формы для входа и регистрации

**Backend делает:**
- Проверяет, правильно ли пользователь ввел пароль
- Сохраняет задачи в базу данных
- Защищает данные — не дает чужим пользователям видеть чужие задачи
- Обрабатывает загрузку аватаров

---

## 🔧 Техническое объяснение

### Frontend (Клиентская часть)

#### Технологии:
- **React 19** — библиотека для создания пользовательского интерфейса
- **Next.js 16** — фреймворк на основе React, который объединяет frontend и backend
- **TypeScript** — язык программирования с проверкой типов
- **Tailwind CSS** — инструмент для стилизации интерфейса
- **Framer Motion** — библиотека для анимаций

#### Что делает Frontend:

1. **Client Components (Клиентские компоненты)**
   - Файлы с директивой `"use client"` в начале
   - Выполняются в браузере пользователя
   - Могут использовать React хуки (`useState`, `useEffect`)
   - Отвечают за интерактивность

   **Пример:** `src/components/TodoApp.tsx`
   ```typescript
   "use client"; // Это клиентский компонент

   export function TodoApp({ todos }) {
     const [filter, setFilter] = useState("all"); // Состояние в браузере

     // Обработчик клика
     const handleClick = () => {
       setFilter("active"); // Изменяет состояние
     };
   }
   ```

2. **Server Components (Серверные компоненты)**
   - Компоненты без `"use client"`
   - Выполняются на сервере перед отправкой в браузер
   - Могут напрямую обращаться к базе данных
   - Не могут использовать браузерные API

   **Пример:** `src/app/[locale]/page.tsx`
   ```typescript
   // Это серверный компонент (нет "use client")
   export default async function HomePage() {
     // Прямой доступ к базе данных на сервере
     const todos = await prisma.todo.findMany({
       where: { userId }
     });

     // Данные уже загружены, когда страница отправляется в браузер
     return <TodoApp todos={todos} />;
   }
   ```

#### Преимущества Next.js App Router:
- **Server-Side Rendering (SSR)** — страницы генерируются на сервере с данными
- **Меньше JavaScript в браузере** — быстрее загрузка
- **Лучше для SEO** — поисковые системы видят готовый HTML

### Backend (Серверная часть)

#### Технологии:
- **Next.js Server Actions** — функции, выполняемые на сервере
- **Prisma ORM** — инструмент для работы с базой данных
- **PostgreSQL** — реляционная база данных
- **NextAuth** — система аутентификации и авторизации
- **Zod** — валидация данных

#### Что делает Backend:

1. **Server Actions (Серверные действия)**
   - Функции с директивой `"use server"`
   - Выполняются только на сервере
   - Имеют доступ к базе данных и файловой системе
   - Обрабатывают формы и запросы от клиента

   **Пример:** `src/app/actions/todos.ts`
   ```typescript
   "use server"; // Это серверная функция

   export async function addTodoAction(formData: FormData) {
     // 1. Проверка авторизации
     const userId = await requireUserId();

     // 2. Валидация данных
     const text = z.string().min(1).parse(formData.get("text"));

     // 3. Сохранение в базу данных
     await prisma.todo.create({
       data: { text, userId }
     });

     // 4. Обновление кэша страницы
     revalidatePath("/");
   }
   ```

2. **База данных (PostgreSQL + Prisma)**
   - **Prisma** — это "переводчик" между кодом и базой данных
   - Вместо SQL запросов пишем простой код на TypeScript
   - Prisma автоматически генерирует типы для безопасности

   **Пример работы:**
   ```typescript
   // Вместо SQL: SELECT * FROM todos WHERE userId = ?
   const todos = await prisma.todo.findMany({
     where: { userId }
   });
   ```

3. **Аутентификация (NextAuth)**
   - Проверяет логин и пароль
   - Создает сессии для пользователей
   - Защищает страницы от неавторизованного доступа
   - Хранит информацию о пользователе в безопасном токене (JWT)

---

## 🏗️ Архитектура проекта

### Структура папок:

```
src/
├── app/                    # Next.js App Router
│   ├── [locale]/          # Страницы с поддержкой языков
│   │   ├── page.tsx       # Главная страница (Server Component)
│   │   ├── profile/       # Страница профиля
│   │   └── auth/          # Страницы входа/регистрации
│   ├── actions/           # Server Actions (Backend логика)
│   │   ├── todos.ts       # Действия с задачами
│   │   ├── auth.ts        # Действия авторизации
│   │   └── profile.ts     # Действия профиля
│   └── api/               # API маршруты (если нужны)
│
├── components/             # React компоненты
│   ├── TodoApp.tsx        # Client Component (интерактивный)
│   ├── ProfileForm.tsx   # Client Component (форма)
│   └── Header.tsx         # Client Component (шапка)
│
├── lib/                   # Вспомогательные библиотеки
│   ├── auth.ts           # Настройка NextAuth
│   ├── prisma.ts         # Подключение к базе данных
│   └── password.ts       # Хеширование паролей
│
└── middleware.ts          # Middleware для проверки авторизации
```

### Разделение ответственности:

| Компонент | Где выполняется | Что делает |
|-----------|----------------|------------|
| `page.tsx` (Server) | Сервер | Загружает данные из БД, передает в компоненты |
| `TodoApp.tsx` (Client) | Браузер | Показывает интерфейс, обрабатывает клики |
| `addTodoAction` (Server) | Сервер | Сохраняет задачу в БД, валидирует данные |
| `prisma.todo.create()` | Сервер | Записывает в PostgreSQL |

---

## 🔄 Поток данных

### Пример: Добавление новой задачи

```
1. ПОЛЬЗОВАТЕЛЬ
   ↓
   Вводит текст в форму и нажимает "Добавить"

2. FRONTEND (TodoApp.tsx - Client Component)
   ↓
   Форма отправляет данные через Server Action
   <form action={addTodoAction}>

3. NETWORK (HTTP запрос)
   ↓
   Данные отправляются на сервер через безопасное соединение

4. BACKEND (addTodoAction - Server Action)
   ↓
   a) Проверка авторизации (requireUserId)
   b) Валидация данных (Zod)
   c) Сохранение в БД (Prisma → PostgreSQL)
   d) Обновление кэша (revalidatePath)

5. DATABASE (PostgreSQL)
   ↓
   Задача сохранена в таблице "todos"

6. BACKEND → FRONTEND
   ↓
   Next.js автоматически обновляет страницу с новыми данными

7. FRONTEND (обновленный интерфейс)
   ↓
   Пользователь видит новую задачу в списке
```

### Пример: Загрузка страницы со списком задач

```
1. ПОЛЬЗОВАТЕЛЬ
   ↓
   Открывает главную страницу в браузере

2. NEXT.JS MIDDLEWARE
   ↓
   Проверяет авторизацию через NextAuth

3. SERVER COMPONENT (page.tsx)
   ↓
   a) Получает сессию пользователя (auth())
   b) Загружает задачи из БД (prisma.todo.findMany)
   c) Генерирует HTML с данными

4. DATABASE (PostgreSQL)
   ↓
   Возвращает список задач пользователя

5. SERVER → CLIENT
   ↓
   Next.js отправляет готовую HTML страницу с данными

6. CLIENT COMPONENT (TodoApp.tsx)
   ↓
   Получает данные как props, делает интерфейс интерактивным

7. ПОЛЬЗОВАТЕЛЬ
   ↓
   Видит страницу с задачами, может взаимодействовать
```

---

## 🔐 Безопасность

### Как защищены данные:

1. **Аутентификация**
   - Пароли хешируются с помощью bcrypt (нельзя восстановить)
   - Сессии хранятся в безопасных JWT токенах
   - NextAuth проверяет каждое действие

2. **Авторизация**
   - Каждый Server Action проверяет `userId`
   - Пользователь видит только свои задачи
   - Защита на уровне базы данных (WHERE userId = ?)

3. **Валидация**
   - Zod проверяет все входящие данные
   - Защита от SQL-инъекций через Prisma
   - Ограничение размера файлов и типов

---

## 📊 Сравнение подходов

### Традиционный подход (REST API):
```
Frontend → HTTP запрос → Backend API → База данных
         ← JSON ответ ←              ←
```

### Наш подход (Next.js Server Actions):
```
Frontend → Server Action → База данных
         ← Автоматическое обновление ←
```

**Преимущества нашего подхода:**
- ✅ Меньше кода (не нужны отдельные API маршруты)
- ✅ Типобезопасность (TypeScript проверяет типы)
- ✅ Автоматическое обновление UI
- ✅ Лучшая производительность (меньше запросов)

---

## 🎓 Ключевые концепции для защиты

1. **Server Components vs Client Components**
   - Server: загрузка данных, безопасность
   - Client: интерактивность, анимации

2. **Server Actions**
   - Выполняются на сервере
   - Имеют доступ к БД
   - Защищены автоматически

3. **Prisma ORM**
   - Типобезопасная работа с БД
   - Автоматическая генерация типов
   - Защита от SQL-инъекций

4. **NextAuth**
   - Управление сессиями
   - JWT токены
   - Защита маршрутов

---

## 💡 Заключение

Наш проект использует современный подход **Full-Stack Next.js**, где:
- Frontend и Backend находятся в одном проекте
- Серверные компоненты загружают данные
- Клиентские компоненты делают интерфейс интерактивным
- Server Actions обрабатывают действия пользователя
- Prisma обеспечивает безопасную работу с базой данных

Это позволяет создавать быстрые, безопасные и удобные веб-приложения с минимальным количеством кода.
